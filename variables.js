//type assertions
var msg;
msg = "msg type now is any";
//prefix the variable with the type in angle brackets and parenthis
var endsWithC = msg.charAt(1);
var alternativeWay = msg.charAt(1);
console.log(alternativeWay);
